# Supabase (public client — protect data with RLS)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Interim admin gate for /admin — bundled into client JS (not a real secret).
# Migrate to Supabase Auth before treating this as production-hardened.
VITE_ADMIN_PASSWORD=change-me
