# Supabase (public client — protect data with RLS)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Admin uses Supabase Auth (email/password) at /admin — no VITE_ADMIN_PASSWORD.
# Create users in Supabase Dashboard → Authentication → Users.
# Apply RLS: supabase/migrations/20260729120000_rls_policies.sql
